import { SortingProductsPipe } from './sorting-products.pipe';

describe('SortingProductsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortingProductsPipe();
    expect(pipe).toBeTruthy();
  });
});
